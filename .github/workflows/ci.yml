name: Storybook PR Checks
on:
  pull_request:
    branches:
      - master

jobs:
  smartui-gihub-action:
    name: Execute Storybook build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1

    - name: Find Last CommitId
      run: |
        API_HOST=https://api.github.com
        # Check out the PR branch
        git checkout $GITHUB_HEAD_REF
        # Get the commit ID of the last commit
        COMMIT_ID=$(git rev-parse HEAD)
        echo "Last commit ID of PR: $COMMIT_ID"
        GITHUB_URL=$API_HOST/repos/$GITHUB_REPOSITORY/statuses/$COMMIT_ID
        echo "GITHUB_URL: $GITHUB_URL"
        echo "GITHUB_URL=$GITHUB_URL" >> $GITHUB_ENV
   
    - name: Install Dependencies
      run: |
        npm install
        npm install @lambdatest/smartui-storybook -g

    - name: Execute storybook build1
      run: |
        export PROJECT_TOKEN=33244476#8aaa8c73-75f1-40b6-b696-c91e8e9d6f6c#Storybook-Test-1
        smartui --version
        smartui storybook ./story-dashlane --config .smartui.json
        
    - name: Execute storybook build2
      run: |
        export PROJECT_TOKEN=33244476#b9d9ab08-4fd1-4f90-b4cf-076666dbe541#Storybook-Test-2
        smartui --version
        smartui storybook ./story-dashlane --config .smartui.json

        















       




